<%= form_for(@tweet=Tweet.new) do |f| %>
  <% if @tweet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tweet.errors.count, "error") %> prohibited this tweet from being saved:</h2>

      <ul>
      <% @tweet.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field" id="entertweet">
    <%= f.text_area :post, id: "newtweet", maxlength: 140 %>
  </div>
  <button type="button" class="actions" id="tweetthis">
    <%= f.submit %>
  </button>
<% end %>
