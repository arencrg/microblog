<p id="notice"><%= notice %></p>

<p>
  <%= image_tag @tweet.user.avatar%> <br/>
  <%= @tweet.user.username %>
</p>

<p>
  <strong>Tweet:</strong>
  <%= @tweet.post %>
  <br/>
  <small><%= @tweet.created_at %></small>
</p>

<% if @tweet.user.username == current_user.username %>
<%= button_to "destroy", @tweet, :method => :delete %>  
<% end %>  

<%= link_to 'Back', tweets_path %>
